function focused() {
    let inputFieldElement = document.getElementsByTagName('input');
    
    inputFieldElement.addEventListener('onfocus', inputFocus);

    function inputFocus (event) {

    }
}